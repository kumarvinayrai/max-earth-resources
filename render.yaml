name: Deploy to Render

on:
  push:
    branches:
      - main  # Or 'master' depending on your default branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Notify Render (Deploy happens via GitHub hook automatically)
      run: echo "Code pushed to Render for deployment"
